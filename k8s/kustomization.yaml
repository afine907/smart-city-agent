apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - config.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  app.kubernetes.io/name: llm-traffic-controller
  app.kubernetes.io/version: "0.1.0"
